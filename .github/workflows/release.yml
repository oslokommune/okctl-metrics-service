name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
          fetch-depth: '1'
    
    - name: Build the Docker image
      run: docker build . --tag okctl-metrics-service:$(date +%s)
